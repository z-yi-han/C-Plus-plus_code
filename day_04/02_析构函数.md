功能与构造函数相反,完成的不是对象本身的销毁(比如局部对象是存在栈帧的,函数结束后栈帧销毁自动释放)，而是完成对象中资源的清理释放工作。(类似Destroy函数)  
析构函数特点
- 析构函数名是类名前加~
- 无参数无返回值(也就是没有函数重载)
- 对象生命周期结束自动调用
- 不写编译器自动生成的析构对内置类型不处理,对自定义类型调用其析构
- 类中没有资源申请时可以不写析构函数,但是有资源申请时一定要写析构函数
- 有多个对象,后定义的先析构(类似于数数据结构的栈,后进先出)
-  MyQueen不需要写析构函数，即便显示写析构也会自动调用对象的析构，防止内存泄露