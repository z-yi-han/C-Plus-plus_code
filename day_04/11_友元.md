- 突破类访问限定符封装的方式,在函数声明或类声明奇前加friend,并且把友元声明放进一个类中
- 友元函数是声明不是函数,friend后面的函数可以访问该类
- 友元函数可以在任何位置声明
- 一个函数可以是多个类的友元
- 有些情况需要前置声明
- 友元类中的成员函数都可以是另⼀个类的友元函数，都可以访问另⼀个类中的私有和保护成员
- 友元类的关系是单向的，不具有交换性
- 友元类关系不能传递
- 友元增加耦合度,破坏封装,不能多用