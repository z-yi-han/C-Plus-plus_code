对于自定义类型对象无法比较大小,必须需要自定义一个函数来比较,因此引出了预算符重载
- 如果自定义类型对象使用预算符时没有对应的预算符重载,那么编译报错
- 预算符重载时具有特殊名字的函数,operator[运算符]构成,具有返回类型,参数列表和函数体
- 重载运算符函数的参数个数和运算符作用的运算对象个数一样多
- 如果把重载运算符函数写作成员函数,那么第一个运算对象默认传给this指针.参数比运算对象少一个
-  .* ::   sizeof   ?:   .  均不能楚重载 
- 重载<<和>>需要重载为全局函数
- 全局成员都有调成员