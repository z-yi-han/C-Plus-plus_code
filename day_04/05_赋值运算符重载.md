默认成员函数,用于完成两个已经存在的对象直接拷贝复制(拷贝构造用于一个对象拷贝初始化另一个对象)
- 赋值运算符重载必须重载为成员函数,而且参数建议写成const当前类类型的引用,防止传值传参的拷贝构造
- 有返回值且建议返回值写成当前类类型的引用,减少拷贝提高效率
- 没有显示实现时系统会默认生成一个对内置类型完成浅拷贝
- 和拷贝构造一样,如果类中没有对资源的指向,那么系统默认生成的即可,也就是说,显示实现析构并释放资源必须显示实现赋值运算符重载