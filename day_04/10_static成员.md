- 用static修饰的成员变量就是静态成员变量,必须在类外初始化
- 静态成员变量为所有类对象共享,存放在静态区,不存在对象里(也就是不走初始化列表)
- 静态成员函数没有this指针，可以指定类域调用
- 静态成员函数只能访问静态成员,因为没有this指针,非静态访问静态随便访问
- 静态成员也是类的成员，受public、protected、private 访问限定符的限制
- 静态成员属于所有类和对象共享
- static变量生命周期是全局的